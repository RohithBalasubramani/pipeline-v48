# layer1b/compare — natural multi-asset compare: split a 'compare A and B' prompt into per-name sub-prompts and
# resolve EACH name through the SAME 1b resolver, so two exact full names auto-compare while a homonym stays a picker.
