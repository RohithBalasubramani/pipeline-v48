"""Group-card path (Approach B): lean atom = own exact_metadata + own data_instructions.fields[], source→shared buffer. PROVISIONAL.

V48 atomic skeleton (fine-grained) — single-purpose scaffold.
"""

# TODO(v48): implement.
