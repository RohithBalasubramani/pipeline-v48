"""Overview — Diesel Generator (widget-envelope).

Stub until specced. DG assets aren't seeded yet and the overview columns
haven't been confirmed, so this advertises the page as pending rather than
emitting null widgets. Mirror LtTransformerOverview once the spec lands.
"""
from .._widgets_base import StubWidgetStrategy


class DgOverview(StubWidgetStrategy):
    pass
