"""Overview — HT Transformer (widget-envelope).

Stub until specced. HT-transformer assets aren't seeded yet and the overview
columns haven't been confirmed, so this advertises the page as pending rather
than emitting null widgets. Mirror LtTransformerOverview once the spec lands.
"""
from .._widgets_base import StubWidgetStrategy


class HtTransformerOverview(StubWidgetStrategy):
    pass
