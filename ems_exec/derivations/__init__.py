"""ems_exec/derivations — the recovery-fn library (moved from lt_panels/derivations; core modules only,
self-contained: config/* + relative imports). window_metrics/trend (aggregate, need services) intentionally excluded."""
from . import registry  # noqa: F401
