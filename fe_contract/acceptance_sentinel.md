# acceptance_sentinel — the LIVE Storybook-sentinel acceptance gate

Mutate ONE `exact_metadata` field at :6008 and read the card DOM moves (§B4). NOT golden-payload comparison — green RTL + byte-identical defaults hid 3 real RTM gaps.
