# mapper_contract — the Snapshot shapes the FE mapper boundary consumes

RealTimeMonitoringSnapshot / PanelHarmonicsPqSnapshot — what `frames/` DATA-fill must target so live and test-db are interchangeable.
