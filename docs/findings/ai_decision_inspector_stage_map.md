# AI Decision Inspector — stage map (workflow wf_148f4d8d, 2026-07-12)

## infra

- **ai_log_record_shape**: Top-level keys of one record (verified from real first lines of both ai_pytest_log.jsonl and ai_r_66c37cc5bb.jsonl): ["request", "response", "run_id", "ts", "url"]. ts = datetime.now().isoformat(); url = the :8200 vLLM URL; request = parsed JSON POST body (keys: chat_template_kwargs, messages, model, response_format, seed, temperature); response = full parsed vLLM response JSON.
- **run_id_threading**: set_run_id is defined in obs/ai_log.py:13 (sets module-global _RUN_ID, default "default") and called in exactly 4 places (excluding __pycache__): (1) run/harness.py:187 — run_pipeline() does `run_id = run_id or make_run_id(prompt)` then `ai_log.set_run_id(run_id)`; (2) run/harness.py:107 — _reflect_loop() re-sets it per reroute attempt with `make_run_id(prompt, salt=f"loop{attempt}")`; (3) tests/conftest.py:19 — `_ai.set_run_id("pytest")`; (4) tests/test_foundations.py:41 — `ai_log.set_run_id("pytest_log")`. Flow from host: host/server.py build_response() (line 102) calls run_pipeline(prompt, asset_id=asset_id) with NO run_id, so harness derives it via run/run_id.py make_run_id(prompt) = "r_" + sha1(salt+"|"+prompt)[:10] (deterministic per prompt). Multi-asset lane run_pipeline_multi (harness.py:329-352) makes a parent run_id plus per-class lane run ids make_run_id(prompt, salt=f"class:{cls}") passed into run_pipeline, which calls set_run_id per lane. llm/client.py reads the current id back via getattr(ai_log, "_RUN_ID", "default") for failure telemetry. Note: _RUN_ID is a process-global — concurrent requests share it.
- **failures_api**: obs/failures.py — `def record(stage, reason, *, card_id=None, group_id=None, detail="", run_id="default")`. Appends one JSON line to outputs/logs/failures_<run_id>.jsonl with shape {"ts": iso, "run_id", "stage", "card_id", "group_id", "reason", "detail" (truncated to 300 chars)} and returns the rec. Called from llm/client.py on every classified LLM failure (stage="llm") and fanned out from obs/stage.py stage() when a stage's own fields signal a defect (ERROR/fail/ok=False/gap/gaps>0 → reasons stage_error/card_fail/exec_fail/fill_gap).
- **llm_config**: llm/config.py: LLM_URL = env override or "http://localhost:8200/v1/chat/completions"; MODEL = env override or "Qwen/Qwen3.6-35B-A3B-FP8". Used by llm/client.py call_qwen (temp 0 + pinned seed 42, json_object/json_schema response_format, thinking off; DB-driven timeouts/retries via app_config).
- **usage_in_response**: Yes. Every logged vLLM response includes a usage object. From the real first line of ai_r_66c37cc5bb.jsonl: usage = {"prompt_tokens": 3710, "total_tokens": 3742, "completion_tokens": 32, "prompt_tokens_details": null}. Same keys in ai_pytest_log.jsonl (prompt_tokens 29 / total 35 / completion 6). Full response keys: choices, created, id, kv_transfer_params, model, object, prompt_logprobs, prompt_token_ids, service_tier, system_fingerprint, usage.
- **latency_captured**: Per-LLM-call latency is NOT captured anywhere: obs/ai_log.py records only a single "ts" per call (no start/duration), and llm/client.py does not time its urlopen. The only latency captured today is whole-run elapsed_ms in host/server.py build_response(): t0=time.time() at line 101, emitted in the RESPONSE stage record (line 135, elapsed_ms=int((time.time()-t0)*1000) → lands in outputs/logs/pipeline_<run_id>.jsonl via obs/stage.py) and in the API response JSON (line 141). obs/stage.py also stamps ts=time.time() per stage record, so inter-stage deltas are reconstructable from pipeline_<run_id>.jsonl, but no per-call or per-stage duration field exists. (copilot/ and host/multi_asset.py have their own elapsed_ms but not per-LLM-call.)
- **entrypoints**: run/ directory: harness.py (main orchestrator: run_pipeline at line 184 — sets run_id=run_id or make_run_id(prompt) then ai_log.set_run_id; run_pipeline_multi at 329 — parent make_run_id(prompt) + per-class-lane make_run_id(prompt, salt="class:<cls>")); run_id.py (make_run_id: "r_"+sha1[:10] of salt|prompt); layer2_all.py (run_2_all(run_id, l1a, l1b) — per-card concurrent L2 fan-out, receives run_id as an argument, never sets it); parallel.py (thread fan-out helper); reflect.py (reroute feedback/note builders); degrade_gate.py; reconcile_granularity.py. host/server.py: imports run_pipeline from run.harness (line 36); build_response(prompt, asset_id, date_window) at line 100 calls run_pipeline(prompt, asset_id=asset_id) WITHOUT passing run_id (so the id is always the deterministic prompt hash), reads it back as out["run_id"] for the RESPONSE stage record and the response JSON, and persists the full response to outputs/logs/response_<run_id>.json (line ~182) keyed to match pipeline_/ai_ logs.
- **sample_log_files**: Newest ai_*.jsonl in /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/outputs/logs/: ai_pytest_log.jsonl 7,956,803 B (Jul 12 02:13); ai_pytest.jsonl 5,710,947 B (Jul 12 02:13); ai_r_66c37cc5bb.jsonl 32,419 B (Jul 11 19:26); ai_r_fd52bf6aee.jsonl 2,374,303 B (Jul 11 19:23); ai_r_6c7e2c6ffd.jsonl 37,074 B (Jul 9 12:49). Plain `ls -la outputs/logs | tail -5` shows response_r_*.json files + stack_monitor.log (the dir also holds response_<run_id>.json, pipeline_<run_id>.jsonl, failures_<run_id>.jsonl).
- **stage_module**: obs/stage.py — stage(run_id, name, **fields): prints "[<run_id>] <name> k=v..." to stderr (lands in outputs/host.log) AND appends {"ts": time.time(), "stage": name, **fields} to outputs/logs/pipeline_<run_id>.jsonl; then a telemetry-only failure fan-out (_failure_signal) mirrors defect-shaped fields (ERROR=/fail=/ok=False/gap=/gaps>0) into obs.failures.record with reasons stage_error/card_fail/exec_fail/fill_gap — never raises, never alters the stage record. obs/notes.py — record(run_id, notes): persists the reflect-loop notes {"loop1": [{card_id,title,answerability,note}], "loop2": str|null} to outputs/notes/<run_id>.json (no-op if both empty) and logs a summary stage(run_id, "notes", loop1=N, loop2=bool, partial=N, gap=N).
- **summary**: pipeline_v48 observability: obs/ai_log.py monkeypatches urllib.request.urlopen at import and tees every :8200 vLLM call to outputs/logs/ai_<run_id>.jsonl with records {ts, run_id, url, request, response}; the run id is a module-global set via set_run_id, called only from run/harness.py run_pipeline (run_id or make_run_id(prompt) = deterministic "r_"+sha1(prompt)[:10]) and per reflect-loop attempt (salt "loopN"), plus two pytest fixtures. host/server.py never passes a run_id — it calls run_pipeline(prompt, asset_id) and reads out["run_id"] back for its RESPONSE stage record, response JSON, and response_<run_id>.json persistence. llm/config.py points at localhost:8200 Qwen/Qwen3.6-35B-A3B-FP8 (env-overridable); llm/client.py call_qwen classifies every failure and records it via obs/failures.record(stage, reason, card_id=, group_id=, detail=, run_id=) → failures_<run_id>.jsonl, with obs/stage.py also fanning defect-shaped stage fields into failures. Logged vLLM responses DO include usage (prompt_tokens/completion_tokens/total_tokens/prompt_tokens_details) in every record, so token accounting is already on disk — but NO per-LLM-call latency is captured anywhere (ai_log stores one ts only); the only timing today is host/server.py's whole-run elapsed_ms (RESPONSE stage + API response) and per-stage ts timestamps in pipeline_<run_id>.jsonl.

## layer1a

### stage `route` — /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/layer1a/route.py:96
- **purpose**: Pick the ONE dashboard page template (page_key) whose card set best tells the story the user prompt asks, and extract the prompt's primary metric, intent, and relative time window.
- **prompt_built_from**: system = layer1a/prompts/system.md with {{METRIC_VOCAB}}/{{INTENT_VOCAB}} substituted (route.py:78-80; contains routing rules + 8 worked near-tie examples). user = 'PAGES:\n' + _candidate_block(specs, titles) (route.py:34-55: shell-grouped via prompts/shell_grouping.md header, one line per page 'page_key | title | story | cards' where story = catalog_compress.merge_story dedup of purpose/theme/answers and cards = card titles from db_reads/card_titles.py capped at cfg route.card_titles_max=400) + '\n\nPROMPT: {prompt!r}' + optional prompts/reroute_clause.md with {feedback} substituted (route.py:82-83) + 'JSON:'
- **candidates**: Four candidate sets, all materialized before the prompt is built. (1) page_key: `specs` at route.py:59 = filter_to_available(read_page_specs(db)) — a list of dicts {page_key,title,purpose,answers,theme,archetype,shell} read from cmd_catalog.page_specs WHERE status='live' (layer1a/db_reads/page_specs.py:5-16) intersected with the allow-list in config/available_pages.py (env V48_AVAILABLE_PAGES > cmd_catalog.routable_pages > AVAILABLE_PAGES code default, ~18 keys); further filtered at route.py:67 by filter_renderable_templates (drops templates over feasibility.template_max_unrenderable_frac) and at route.py:71-73 by the reflect-loop exclude_page_key removal; flattened to `keys = [s['page_key'] for s in specs]` at route.py:75 — this exact list is both the PAGES prompt block (via _candidate_block, route.py:34-55) and the guided-decoding page_key enum. (2) metric: METRIC_VOCAB at config/metrics.py:7 (cfg 'metrics.vocab', default ['current','voltage','power','energy','thd','pf','frequency','temperature']), injected into system.md as {{METRIC_VOCAB}} at route.py:79. (3) intent: `_intent_vocab` at route.py:95 (cfg 'intents.vocab', default ['trend','distribution','snapshot','table','events']), injected as {{INTENT_VOCAB}} at route.py:80. (4) window: `window_enum` at route_schema.py:59 = list(config/windows.TIME_WINDOWS keys: today, last-24h, last-7-days, shift-8h, live) + ['none'] sentinel.
- **selected_field**: page_key (the page selection; validated/recovered by resolve_page_key at route.py:103 → parse/page_key_fallback.py, how ∈ verbatim/segment/substring, else fail-closed RuntimeError), plus metric + intent (clamped to vocab by clamp_metric_intent, route.py:108) and window (clamped to TIME_WINDOWS preset or None by clamp_window, route.py:109)
- **reasoning_field**: none — the taught reply shape and route_answer_schema contain only the 4 selection keys; no rationale key exists. routing.page_key_how / dropped_templates / excluded_page_key telemetry in the return dict (route.py:114-115) is computed post-hoc by Python, not emitted by the model.
- **confidence_field**: none
- **output_shape**: Flat 4-key JSON object. Taught shape (prompts/system.md line 34): {"page_key":"<exact page_key>","metric":"<one keyword>","intent":"<one intent keyword>","window":"<one preset or none>"}. Explicit guided-decoding schema exists — route_schema.py:62-71 (attached only when app_config llm.guided_json.route is on, default OFF): {"type":"object","properties":{"page_key":{"type":"string","enum":list(keys)},"metric":{"type":"string","enum":list(metric_vocab)},"intent":{"type":"string","enum":list(intent_vocab)},"window":{"type":"string","enum":window_enum}},"required":["page_key","metric","intent","window"]}
- **error_handling**: call_qwen default on_error='empty' (fail-open {}), but route() converts to FAIL-CLOSED: empty response raises RuntimeError with the 'llm transport/parse failure' fingerprint (route.py:98-102) so degrade_gate surfaces data_unavailable instead of a keys[0] misroute; unresolvable page_key also raises (route.py:104-107). Inside llm/client.py: one bounded parse-retry (llm.parse_retry=1, error text appended), no-retry kinds timeout/truncated, prompt-budget preflight ~45000 tok, per-stage timeout llm.timeout.route, temp 0 + pinned seed 42, json_object response_format unless the guided flag promotes json_schema.
- **notes**: route_to() at route.py:118 is the deterministic sibling (granularity-reconcile) with NO LLM call. The determinism story: enum-constraining page_key removes the fuzzy segment/substring recovery branch so near-tie routes can't flip under vLLM batch load; flag seeded off by db/seed_route_guided_json.sql.

### stage `stories` — /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/layer1a/story_builder.py:37
- **purpose**: Write a one-sentence per-card analytical_story for every card on the routed page — what story each card tells for this prompt given its role/purpose (generative annotation, not a selection).
- **prompt_built_from**: system = layer1a/prompts/story_instruction.md (static, no substitutions). user = f-string at story_builder.py:31-32: 'PROMPT: {prompt!r}\nMETRIC: {metric}  INTENT: {intent}\nPAGE: {page_key}\nCARDS:\n{listing}\nJSON:' — metric/intent/page_key come from the upstream route stage result (wired via layer1a/build.py:10).
- **candidates**: none (not a selection-type decision) — but the card set it annotates is materialized as `cards` at story_builder.py:23 = read_page_cards(page_key, db) (layer1a/db_reads/cards_intent.py:9-65, a join of page_layout_cards + cards + card_grid_size + card_data_recipe + card_handling on cmd_catalog), rendered into the prompt as the `listing` block at story_builder.py:26-29 ('- card {card_id} | {title} | role: {analytical_role} | purpose: {card_purpose[:80]}').
- **selected_field**: none — output is the 'stories' dict keyed by bare numeric card_id; values are assigned onto each card as c['analytical_story'] at story_builder.py:46-49 (key normalized by _norm_id which tolerates 'card 44'/'44'/44; missing card → '')
- **reasoning_field**: none as a separate key — each story sentence is itself the rationale text; no reasoning/rationale field in the taught shape
- **confidence_field**: none
- **output_shape**: {"stories": {"<bare numeric card_id>": "<one sentence>", ...}} — taught verbatim in prompts/story_instruction.md line 5: JSON only: {"stories": {"44": "<one sentence>", "45": "<one sentence>"}}. No explicit JSON schema object exists for this stage (plain json_object response_format; no json_schema kwarg passed).
- **error_handling**: on_error='marker' — a transport/parse failure returns {"_llm_error": kind, "_llm_error_detail": ...} instead of {}; story_builder.py:38-45 detects the marker, records obs.failures('layer1a','stories_llm_failed') telemetry, then degrades gracefully: every card gets analytical_story='' (fail-open per-leaf, pipeline continues). Per-stage timeout row llm.timeout.stories; same client-level parse-retry/no-retry/budget rules as all call_qwen sites.
- **notes**: layer1a/partition_inputs/*.py (card_combo, card_link, interdependency_prose, page_control) are pure DB readers feeding partition/group_detect via build.py — no LLM, no prompt-building. prompts/ holds only the 4 .md files (system, story_instruction, reroute_clause, shell_grouping).

**summary**: layer1a has exactly two call_qwen sites. (1) stage='route' at layer1a/route.py:96 — the storytelling router: candidate pages exist as `specs` (route.py:59, from cmd_catalog.page_specs filtered by config/available_pages.py, then feasibility-gated at :67 and reroute-excluded at :71-73) and as `keys` (route.py:75), which doubles as the enum in the explicit guided-decoding schema route_answer_schema (route_schema.py:62-71: page_key/metric/intent/window all enum-pinned, all required, flag llm.guided_json.route default OFF). Metric candidates = METRIC_VOCAB (config/metrics.py:7), intents = cfg('intents.vocab') (route.py:95), windows = TIME_WINDOWS keys + 'none' (config/windows.py:4, route_schema.py:59). Output keys: page_key (selection, fail-closed via resolve_page_key), metric, intent, window; no reasoning or confidence keys anywhere (routing.page_key_how telemetry is computed in Python). (2) stage='stories' at layer1a/story_builder.py:37 — generative per-card story writer over `cards` (story_builder.py:23 from read_page_cards); output {'stories': {'<card_id>': '<one sentence>'}} per prompts/story_instruction.md; no schema, no selection/reasoning/confidence keys; on_error='marker' with graceful degrade to empty stories, whereas the route stage raises (fail-closed) on any LLM failure. partition_inputs/ files are LLM-free DB readers for card-coupling partitioning.

## layer1b

### stage `asset_resolve` — /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/layer1b/resolve/asset_resolve.py:157
- **purpose**: PURE-AI asset resolution: decide which lt_mfm registry asset(s) the prompt means — one confident pin, an ambiguous candidate list for the AssetPicker, or empty (pure-metric prompt).
- **prompt_built_from**: system = layer1b/prompts/asset_system.md loaded via _load_prompt at :146 (verbatim-name contract, unit-number-decisive, class-binding, homonym=ambiguous, NO-DATA preference, incomer/outgoing-is-not-a-class rule); user built inline at :147-149 = CANDIDATES TSV listing + 'CLASSES PRESENT IN THE REGISTRY: <live vocab>' + PROMPT repr + 'JSON:'.
- **candidates**: Full asset registry materialized at asset_resolve.py:68 as `cands = asset_candidates()` (builder: layer1b/resolve/asset_candidates.py:100-125, list of rows [id, name, table_name, mfm_type_id, load_group, class, has_data, has_feeders, never_wired, table_exists, aka, loc]). Class-prior narrowing at :86-87 (`prior = class_from_subject(prompt)`, `listed = candidates_of_class(cands, prior)`). Serialized for the prompt at :141-143 as `listing` — TSV 'name<TAB>class<TAB>load_group<TAB>NO-DATA-flag<TAB>aka' with the registry id column deliberately HIDDEN (id is off-by-one vs unit numbers; model resolves by NAME only). Deterministic name→row mapping back via `by_name`/`by_norm`/`by_alias` dicts built at :94-106 and `resolve_name()` at :108-119 (exact → normalized → unique alias; collisions → None → ambiguous). Live class vocabulary injected as `classes_present` at :145.
- **selected_field**: `names` (confident-pin branch: verbatim registry name(s), first pick used at :168/:179-182) and `candidates` (ambiguous branch: verbatim names mapped to rows at :169, surfaced via ambiguous_candidates at :204). Names are mapped back to registry rows deterministically — the id is looked up, never emitted by the model.
- **reasoning_field**: none (the answer contract is names/confident/candidates only; no rationale key)
- **confidence_field**: `confident` (boolean, not a score) — read at :165 with bool(res.get("confident", False)); ABSENT KEY DEFAULTS FALSE (a half-parsed emission is never a confident nothing). confident:true + resolvable non-ghost name → confident_pin; confident:false (or unresolvable/collision) → the AssetPicker ambiguity flow (ambiguous_candidates outcome {asset:None, how:'ambiguous', candidates:[as_asset,...]}); confident:true + names:[] → how='empty' only when no class was implied, else empty_fallback browse picker.
- **output_shape**: {"names": [str], "confident": bool, "candidates": [str]} — three taught shapes: confident {"names":["<exact>"],"confident":true}; ambiguous {"confident":false,"candidates":[...]}; empty {"names":[],"confident":true}. Optionally grammar-enforced by ASSET_ANSWER_SCHEMA (answer_schema.py:23-31; only "confident" required — requiring "names" was proven to flip ambiguous answers into arbitrary pins) when app_config flag llm.guided_json.asset_resolve is 'on' (DEFAULT OFF; asset_answer_schema() at answer_schema.py:36-44 returns None otherwise, making call_qwen's json_schema kwarg inert).
- **error_handling**: call_qwen default on_error='empty' (fail-open {} on ANY transport/parse error; llm/client.py:103). Wrapped in retry_once (layer1b/guardrail/retry_one.py:10): calls once, retries EXACTLY ONCE if the result is falsy, returns (result, llm_failed) where llm_failed=True only when BOTH attempts were falsy. On llm_failed → honest degrade at :160-163: empty_fallback(prompt, rows=class-narrowed rows if a prior exists) opens the browse picker instead of a silent how='empty'. stage='asset_resolve' keys per-stage timeout app_config row llm.timeout.asset_resolve (base llm.timeout fallback) plus failure telemetry.
- **notes**: Ambiguity/AssetPicker flow: confident:false → ambiguous_candidates.py dedups by registry id, stable-sorts data-bearing first, projects via as_asset; user pick round-trips as PIPELINE_ASSET_ID → pinned_skip at :73-81 bypasses the LLM entirely. Guardrails around the call (all deterministic, no LLM): _is_ghost (:45-49) blocks confident pins on table_exists=False stubs; spelling_recovery.fuzzy_rows (:186-191) recovers paraphrased/typo'd emitted names as AMBIGUOUS candidates, never a pin; same_family_gate.class_mismatch is telemetry-only, stamped in _finish (:125-136) with class_prior + llm_failed; member_scope(prompt) stamps incomer/outgoing on the resolved panel. The deterministic name-collision gate was DELETED 2026-07-09 (:171-178 comment) — the model now owns the end-user outcome. Multi-asset compare (layer1b/compare/) contains NO call_qwen: detect.py/discriminators.py are lexical+registry; resolve_names.py re-invokes THIS stage once per named asset concurrently (run/parallel) with other names stripped from each sub-prompt, counting a name confident only when how ∈ {AI, user-choice, no_data, collision_gate_fullname} with no candidate list.

### stage `basket` — /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/layer1b/basket/column_basket.py:68
- **purpose**: Card-agnostic GENEROUS column-basket selection: from the resolved meter's real column dictionary, pick every column that could serve the prompt (feasible) plus a ranked best-first shortlist (probable) with confidence and substitutions.
- **prompt_built_from**: system = layer1b/prompts/column_system.md loaded via _load_prompt at :58 (verbatim column-name contract, generous feasible basket, ranked probable with confidence/substitute_for, same-quantity-family zero-fabrication rule); user built at :60-62 = PROMPT repr + METRIC (normalize_metric(prompt) hint, :55) + INTENT + GUIDANCE (DB row app_config layer1b.basket.quality_guidance, code-default _QUALITY_GUIDANCE at :19-27 — the quality-columns inclusion rule) + COLUMNS lines + 'JSON:'.
- **candidates**: Real column dictionary of the resolved asset's neuract table, materialized at column_basket.py:56 as `cols = col_dict(table)` (layer1b/basket/col_dict.py — built from the REAL consumer/neuract schema, rows (column_name, label, kind, unit)); windowed has_data flags at :45 `hasdata = window_nonnull(table)` (last-N-rows non-null, DB knob layer1b.has_data_window_rows); for empty aggregate panels with feeders, the table is swapped to a representative data-bearing feeder's schema at :50-54 (`feeder_table(asset["mfm_id"])`, asset_candidates.py:128-152 BFS). Serialized for the prompt at :57 as `lines` — 'column_name | label | kind | unit | Y/N' one per column.
- **selected_field**: `feasible` (array of column names — filtered to the real set at :80 `[c for c in (res.get("feasible") or []) if c in realset]` so hallucinated names are dropped; then UNIONed after the deterministic logged-floor columns at :79-87, capped by DB knob layer1b.basket.max_columns at :91-92) and `probable[].column` (must be a real column_name, filtered at :96-97).
- **reasoning_field**: `probable[].why` (per-column rationale, defaulted to "" at :104); plus `probable[].substitute_for` (:106) — the asked-for concept a low-confidence stand-in column replaces, so Layer 2 can note substitutions.
- **confidence_field**: `probable[].confidence` — float 0.0–1.0, parsed at :99-102 (default 1.0, clamped to [0,1] at :105). Prompt contract: 1.0 = column IS the asked-for quantity; 0.6–0.8 = best available same-quantity-family stand-in; never cross-family (fuel/oil/coolant concepts left out entirely).
- **output_shape**: LLM emission: {"feasible": [str], "probable": [{"column": str, "label": str, "why": str, "rank": int, "confidence": float, "substitute_for": str?}]}. Function return (:124-128): {"tables": [table], "columns": [{table, column, label, kind, unit, has_data, derivable?, derive_sources?}], "probable": [...normalized...], "n_columns": int, "metric_hint": str, "llm_failed": bool}.
- **error_handling**: Same wrapper as asset_resolve: retry_once(lambda: call_qwen(system, user, stage="basket")) at :68 — call_qwen fail-open {} (on_error default 'empty'), one bounded retry, (res, llm_failed) returned. No json_schema/guided decoding on this stage. On llm_failed the basket degrades to the deterministic LOGGED FLOOR only (:78-79: every windowed-has-data metric column with kind != 'event', DB knob layer1b.basket.include_logged_floor) — llm_failed rides in the returned basket as telemetry (:128) and is surfaced via layer1b contract_problems (schema.validate_layer1b_output), never a gate. stage='basket' keys per-stage timeout llm.timeout.basket. Guard at :39-40: no resolved table → empty basket {tables:[],columns:[],probable:[],n_columns:0,llm_failed:False} without calling the LLM.
- **notes**: Post-selection deterministic layers: floor ∪ AI-feasible union (floor first, order-stable dedup, :83-87) guarantees Layer 2 can bind every real logged column even if the AI narrows; cap default 400 (:91) guards ~190-col lt_panels tables; avg-from-phase derivability (:112-123, knob layer1b.basket.derive_avg_from_phase): a present-but-empty avg column with logged per-phase siblings gets {derivable:'avg_from_phase', derive_sources:[...]} while has_data stays False (recovery pointer, no fabrication). layer1b/guardrail/ has no AI: retry_one.py (the wrapper), spelling_recovery.py (difflib), same_family_gate.py (Counter majority telemetry).

**summary**: layer1b has exactly TWO AI call sites, both fail-open call_qwen wrapped in retry_once (defined at layer1b/guardrail/retry_one.py:10 — semantics: res = call() or {}; return immediately if truthy; else retry exactly once; returns (result_or_{}, llm_failed) where llm_failed=True only when both attempts were falsy; since call_qwen's default on_error='empty' collapses every transport/parse error to {}, retry_once is the layer that distinguishes 'model chose to emit nothing' from 'model was never heard'). (1) asset_resolve (resolve/asset_resolve.py:157): candidates = asset_candidates() registry rows materialized at :68 (asset_candidates.py:100), class-prior-narrowed at :86-87, rendered as an id-HIDDEN name/class/load_group/NO-DATA/aka TSV at :141-143; system prompt = prompts/asset_system.md; output {names, confident, candidates} with confident (bool) defaulting False when absent — confident:true+resolvable name → pin, else the AssetPicker ambiguity flow (ambiguous_candidates dedup + data-bearing-first sort; picker choice round-trips as PIPELINE_ASSET_ID → pinned_skip bypasses the LLM); llm_failed → empty_fallback browse picker; optional guided-JSON grammar via answer_schema.py ASSET_ANSWER_SCHEMA (flag llm.guided_json.asset_resolve, default OFF, only 'confident' required). No numeric confidence key exists — 'confident' is boolean; the graded confidence lives in the basket stage. (2) basket (basket/column_basket.py:68): candidates = real column dictionary col_dict(table) at :56 (+windowed has_data :45, feeder-schema substitution :50-54) rendered as 'name | label | kind | unit | Y/N' lines at :57; system prompt = prompts/column_system.md + DB-driven quality guidance; output {feasible:[cols], probable:[{column,label,why,rank,confidence,substitute_for}]} — feasible filtered to real columns and unioned with a deterministic logged floor, probable carries confidence 0.0-1.0 (clamped) and substitute_for reasoning; llm_failed degrades to floor-only basket, telemetry not a gate. compare/ (detect.py, discriminators.py, resolve_names.py) and guardrail/ (retry_one.py, spelling_recovery.py, same_family_gate.py) contain ZERO call_qwen sites — all deterministic; compare/resolve_names.py fans the asset_resolve AI stage out per named asset (stripped sub-prompts, run/parallel) for natural compares.

## layer2

### stage `l2_emit (initial call)` — /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/layer2/emit/emit.py:209
- **purpose**: The ONE per-card Layer-2 AI call: decide KEEP vs SWAP for the slot (PART 1) and author the card's payload as two halves — exact_metadata (or a morphs map in morphmap mode) plus the data_instructions fill recipe binding each payload leaf to a real DB column/derivation (PART 2).
- **prompt_built_from**: SYSTEM (emit.py:_system, lines 148-194): layer2/prompts/data_instructions_v2.md — the single Layer-2 contract (rules R1-R14 + PART 1 keep/swap + PART 2 morph-emit; it subsumed the retired swap.md/metadata.md/data_instructions.md trio) — plus, when emit.morphmap_mode flag is on AND the card has a stored payload_stripped skeleton, layer2/emit/morphmap/prompt.md appended as a 'PART 2 OVERRIDE — MORPH-MAP' section; placeholders substituted: {{LIVE_ENDPOINTS}}/{{RETIRED_ENDPOINTS}} (run-constant, keeps vLLM prefix cache), conditional <!--ROSTER:BEGIN/END--> section kept only for member-scope cards (_wants_roster_section), {{RECOVERY_LIBRARY}} generated live from the derivation registry with per-card basket/nameplate/breaker filtering. USER (layer2/emit/user_message.py:build_user → _build): run_id/card_id/page_key header; 1a page story + this card's analytical story angle + metric/intent + template_card_ids; 1b asset + NAMEPLATE/DATA WINDOW/equipment-registry fact lines; DB SCHEMA basket lines with qty= quantity-wall tags; 1b ranked RELEVANT COLUMNS with confidence + substitute_for; PANEL MEMBERS block; the cmd_catalog catalog_row (title, handling_class, contract, endpoint hint from domain_endpoints, recipe + verbatim roster_spec, controls, capabilities, feasibility); the stored stripped metadata skeleton (card_payloads.payload_stripped — NULL fails loudly); the FILLABLE DATA-LEAF SLOTS catalog (build_slot_catalog); the SWAP CANDIDATES block; shared-context ref for group cards; rebuilt compacted when > emit.prompt_char_budget (36000 chars). On a gate-retry, emit()'s feedback kwarg appends the exact gate defect strings to the user message (emit.py:204-207).
- **candidates**: swap_decision candidates = the per-slot swap pool, materialized as a Python list of dicts {card_id, title, analytical_role, card_purpose, visualization, width_px, height_px} by layer2/swap/candidates.py:pool() (lines 57-115: SQL over cmd_catalog card_grid_size JOIN cards JOIN card_feasibility; filters = status='live', feasibility verdict in DB knob feasibility.pool_verdicts default ['render_real'], ±15% width AND height (swap.size_tolerance=0.15), off-page, not in 1a template_card_ids, not self, on an available page (_available_card_ids), registered FE renderer (grounding.swap_settle.is_registered) + recoverable default payload (grounding.default_assemble.has_default); soft metric-affinity re-rank then truncate to swap.swap_pool_max=6). Attached to the card input as card_in['swap_candidates'] at layer2/card_input.py:46-49; a swapped-in target gets swap_candidates=[] (card_input.py:69, slot settled). Rendered into the USER prompt at layer2/emit/user_message.py:230-232 (cands lines) under the 'SWAP CANDIDATES (±15% size, render_real, off-page, not in template)' header at user_message.py:401. Secondary closed candidate sets the same call selects from: the DB SCHEMA column basket (card_in['column_basket'], rendered by _basket_lines user_message.py:32-65) for fields[].column; the RECOVERY LIBRARY fn list generated live from ems_exec.derivations.registry.catalog() into the {{RECOVERY_LIBRARY}} placeholder (emit.py:60-120, per-card basket-filtered) for fields[].fn; and the closed endpoint set {{LIVE_ENDPOINTS}}/{{RETIRED_ENDPOINTS}} from layer2/emit/data/endpoint_registry.py + consumer_binding.py (emit.py:134-142) for ems_backend.endpoint.
- **selected_field**: swap_decision.action ('keep'|'swap') with swap_decision.swap_to_id + swap_to_title naming the chosen candidate (must be a listed pool card_id — gate_pool_valid) and swap_decision.cascade[] for coupled combo swaps; within data_instructions: fields[].column (verbatim basket column), fields[].fn (verbatim RECOVERY LIBRARY value_key), ems_backend.endpoint (one of the closed set), roster[] column choices
- **reasoning_field**: swap_decision.reason + swap_decision.criterion (must quote the concrete story-angle word; vague criteria are gate-rejected by gate_vague_reject); also data_note (one human-facing sentence when answerability is partial/none) and data_instructions._honest_blanked[] per-leaf reasons
- **confidence_field**: swap_decision.confidence — a swap is honored only at >= swap.min_confidence (0.90), enforced deterministically post-hoc by layer2/swap/gate_confidence.py:ok(); forced renderability swaps are stamped FORCED_SWAP_CONFIDENCE (>1.0) so the settle post-pass never reverts them
- **output_shape**: Layer2CardOutput (strict JSON, envelope printed verbatim at layer2/prompts/data_instructions_v2.md:117): {card_id, $ctx, render_slot, analytical_story, swap_decision:{action, origin, swap_to_id, swap_to_title, confidence, criterion, reason, cascade[]}, exact_metadata:{..., _morphed:[]} (rewritten to "morphs":{} when the morphmap PART 2 override is composed, emit.py:192-193), data_instructions:{payload_shape, orientation, entity_dim, selection_dim, selection_role, binding:{asset_id,table,ts_col,panel_id,nameplate_scope}|null, window:{lookback,sampling,time_mode}, ems_backend:{endpoint, window_seconds, interval_seconds, sample_count, range, start, end, sampling, metrics[], selection}, fields:[{slot, kind(raw|bucketed|time|derived|const|text|event), role, metric, column, label, unit, agg, source(live|test-db|const|$ctx), value?, fn?, base_columns?, target_column?, sampling?, edge?, filters_table, has_data}], roster?[], _honest_blanked?[]}, controls, answerability('full'|'partial'|'none'), data_note, conforms, failure:{stage,reason,detail}|null}
- **error_handling**: call_qwen(sysmsg, user, stage='l2_emit', on_error='marker') — 'marker' mode returns {'_llm_error': kind, '_llm_error_detail': ...} instead of a silent {} (llm/client.py:78-83); the stage tag selects the DB timeout row llm.timeout.l2_emit and classifies failures via obs.failures. Never raises; emit() returns raw or {} at line 218.
- **notes**: CARD IDENTITY: a card is identified by its numeric card_id (cmd_catalog cards.id) throughout — the fan-out dict is keyed by card_id, the user message headers 'RUN: … CARD: {card_id} PAGE: {page_key}', the output echoes card_id, and swap targets are card_ids; title appears only as a catalog_row fact line and in candidate listings. FAN-OUT: run/layer2_all.py:run_2_all builds one thunk per 1a card (line 40, lambda closing over card_id → layer2/build.py:run_card) and runs them via run/parallel.py:run_parallel (ThreadPoolExecutor), max_workers capped by DB knob layer2.emit_concurrency, code-default 4 (layer2_all.py:48-49) — vLLM contention fix; per-card exceptions are captured (_err) so one bad card never sinks the page. A slot can trigger up to 3-4 total emits: initial emit (build.py:713), a swap-target re-emit for the target's different shape (build.py:737-739, via build_swap_target_input which inherits the slot's story but brings the target's own catalog_row), and one gate-failure re-emit with feedback (build.py:_finalize_with_gate_retry, DB knob llm.gate_retry=1). Post-fan-out, grounding.swap_settle.settle runs a deterministic highest-confidence-first collision revert (two slots swapping to the same target). The AI's swap_decision is only advisory: layer2/swap/decide.py:gate re-derives action/origin deterministically through 6 gates (confidence>=0.90, vague-criterion reject, pool-membership, no-dup, template-dedup, combo cascade) and the renderability enforcer (gate_force_renderable) can force-swap regardless of the AI when the current card is statically unrenderable or the AI itself declared answerability='none'.

### stage `l2_emit (bounded transport retry)` — /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/layer2/emit/emit.py:217
- **purpose**: Identical re-issue of the same per-card emit after a TRANSIENT transport failure — not a different decision, the same call repeated.
- **prompt_built_from**: byte-identical sysmsg + user variables computed once before line 209 — the retry passes the SAME two strings; only the feedback kwarg path (a separate call from build.py's gate retry, not this site) ever changes the prompt
- **candidates**: same materialized card_in['swap_candidates'] list — the prompt (sysmsg, user) is byte-identical to the line-209 call; nothing is rebuilt
- **selected_field**: same as the initial call (swap_decision.action/swap_to_id, fields[].column/fn, ems_backend.endpoint)
- **reasoning_field**: same (swap_decision.reason/criterion, data_note, _honest_blanked)
- **confidence_field**: same (swap_decision.confidence)
- **output_shape**: same Layer2CardOutput JSON
- **error_handling**: This IS the error handling: the while loop (emit.py:215-217) re-calls call_qwen with identical args only when raw carries _llm_error AND the error kind is NOT in llm.no_retry_kinds (default 'timeout,truncated' — those are DETERMINISTIC for this prompt, retrying only doubles the wall-clock hang, the card-5 heatmap lesson) AND retries remain (llm.transport_retry, default 1). So the ONLY differences vs line 209: it runs at most once, only after a classified transient failure (transport/5xx), and never for timeout/truncated. Same stage tag, same on_error='marker'.
- **notes**: Do not confuse this transport retry with the gate-failure re-prompt in layer2/build.py:_finalize_with_gate_retry (build.py:758-779) — that one is a NEW emit(ci, feedback=[...]) call whose user message differs (gate defect strings appended), bounded by llm.gate_retry=1, and explicitly skips 'llm'-stage failures because emit() already did this bounded transport retry.

**summary**: Layer-2 emit is ONE AI call per card (stage 'l2_emit', /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/layer2/emit/emit.py). System prompt = layer2/prompts/data_instructions_v2.md (the single R1-R14 contract; the old swap/metadata/data_instructions trio is retired) + optional morphmap PART 2 override + live-generated {{RECOVERY_LIBRARY}} + endpoint-set placeholders + conditional ROSTER section; user message = layer2/emit/user_message.py (1a story, 1b asset/basket with qty= walls, catalog_row, stripped payload skeleton, slot catalog, SWAP CANDIDATES block). The swap_decision candidate set is materialized as a Python list of dicts by layer2/swap/candidates.py:pool() (SQL over cmd_catalog: ±15% size, render_real, live, off-page/off-template, available-page, registered-renderer + recoverable-default, metric-affinity re-rank, cap 6) and attached at layer2/card_input.py:46 as card_in['swap_candidates']. Selection keys: swap_decision.action/swap_to_id/swap_to_title/cascade; reasoning keys: swap_decision.reason + criterion (plus data_note/_honest_blanked); confidence key: swap_decision.confidence, deterministically gated at >=0.90 (layer2/swap/gate_confidence.py, config swap.min_confidence) alongside 5 other gates in layer2/swap/decide.py plus a renderability force-swap enforcer. The two call_qwen sites: emit.py:209 is the initial call; emit.py:217 is a bounded TRANSPORT retry with byte-identical arguments — it fires at most llm.transport_retry (1) times, only when the returned marker {'_llm_error': kind} names a transient kind NOT in llm.no_retry_kinds (default 'timeout,truncated', which are deterministic and fail fast). Both use stage='l2_emit' (timeout row llm.timeout.l2_emit) and on_error='marker'. A separate gate-failure re-emit (feedback appended to the user message) lives in layer2/build.py:_finalize_with_gate_retry (llm.gate_retry=1). Fan-out: run/layer2_all.py:run_2_all builds one thunk per 1a card keyed by numeric card_id and runs them through run/parallel.py's ThreadPoolExecutor capped by DB knob layer2.emit_concurrency (default 4); a card is identified by card_id everywhere (user-message header 'CARD: {card_id}', output card_id, swap targets by id) — title is descriptive context only. Swap targets trigger a second emit for the target's shape; grounding/swap_settle.settle then deterministically reverts duplicate-target collisions. GROUNDING IS DETERMINISTIC: grep for call_qwen/qwen/llm/8200/openai/anthropic across grounding/*.py returns ZERO hits — grounding/ (swap_settle, schema_route, schema_fingerprint, default_assemble, exemplar_reduce, meaningful, role_scrub, event_skeleton_scrub, measured_annotation_scrub) contains no LLM usage at all; the memory phrase 'AI-grounding' refers to grounding OF the AI's emission (deterministic gates/scrubs applied to LLM output), not an AI call inside grounding/.

## misc

### stage `knowledge_ems` — /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/knowledge/ems.py:75
- **purpose**: ONE combined route+answer+reject call: classifies the user prompt as dashboard / knowledge / off_scope and, for knowledge, writes the educational electrical-mechanical answer in the same call.
- **prompt_built_from**: System = knowledge/prompts/ems.md (read from disk each call via _prompt()); user = _user_message() folding prior knowledge turns (history, oldest-first {prompt,answer}) in front of 'PROMPT: <text>'. Refusal line is DB-driven: app_config knowledge.refusal_line.
- **candidates**: The 3-kind closed vocabulary, materialized as Python tuple _KINDS = ('dashboard','knowledge','off_scope') at knowledge/ems.py:16 and re-embedded as the enum in _SCHEMA at ems.py:21. Not an entity-selection stage — no candidate list of assets/cards is built.
- **selected_field**: kind (enum choice) plus answer (free text used when kind='knowledge')
- **reasoning_field**: none
- **confidence_field**: none
- **output_shape**: {"kind": "dashboard"|"knowledge"|"off_scope", "answer": "<text>"} — enforced via schema= kwarg to call_qwen (json_object + schema); ask() re-wraps to {'kind','answer','refused'}
- **error_handling**: Goes through llm/client.py call_qwen (stage='knowledge_ems') so it is fail-open at transport; ask() additionally wraps in try/except and FAIL-OPENs to kind='dashboard' on ANY exception or invalid kind; empty knowledge answer degrades to the refusal line (refused=True). No retry wrapper. Valve: app_config knowledge.enabled ('on' unless 'off'). Invoked from host/server.py:316-323 only for fresh prompts (skipped when asset_id/asset_ids pinned).
- **notes**: Uses the sanctioned call_qwen path on :8200, so it IS logged by obs/ai_log to outputs/logs/ai_<run_id>.jsonl. knowledge/route.py from memory NO LONGER EXISTS — routing was consolidated into this single file.

### stage `insight_narrator (assigned — no stage label passed; direct urllib POST, not call_qwen)` — /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/ems_exec/renderers/_insight.py:122
- **purpose**: Narrates a PRE-JUDGED story dict (all numbers/verdicts already computed in Python by the _story/ page builders) into one factual AI-summary sentence per requested field for the AI Summary cards 8/19/25/28 — pure narration, no decision-making over data.
- **prompt_built_from**: Hardcoded _SYSTEM template in _insight.py:75-87 ({fields} substituted) + user = json.dumps(story) with a 'narrate, do not judge' preamble (_messages, :90-95). Story itself comes from the _story builders over real neuract facts, dispatched by narrative_ai.render (ctx page_key / CARD_PAGE card-id fallback), which the host reaches via ems_exec.renderers.run_special for handling_class='narrative_ai' (host/exec_cards.py:69,117).
- **candidates**: none — generative narration, not a selection. Input facts are the story dict built by ems_exec/renderers/_story/{energy_distribution,voltage_current,harmonics_pq,real_time_monitoring}.py (BUILDERS registry in _story/__init__.py:30-36, keyed on page_key).
- **selected_field**: none — output is one sentence per requested field; the only consumer narrative_ai.py passes fields=['text'] and reads result['text']; the 'badge' verdict is computed in Python and never touched by the model
- **reasoning_field**: none — thinking explicitly disabled at the source via chat_template_kwargs {enable_thinking: false}; a stray dangling </think> is stripped by _strip_think
- **confidence_field**: none
- **output_shape**: {<field>: "<one factual sentence>", ...} strict JSON (response_format json_object), exactly the requested fields; any missing/empty field voids the WHOLE result (fall back wholesale)
- **error_handling**: DOES call :8200 directly — urllib.request.urlopen POST to LLM_URL+/chat/completions (default http://localhost:8200/v1, model Qwen/Qwen3.6-35B-A3B-FP8), BYPASSING llm/client.py call_qwen. Logging is still wired: it imports obs.ai_log whose urlopen monkeypatch logs every ':8200' call to outputs/logs/ai_<run_id>.jsonl. ANY failure (down/slow/bad JSON/missing field) → None → caller's deterministic fallback dict verbatim (narrative_ai wraps in a second try/except too). Content-hash cache (_CACHE, max 2048, cleared when full) so the model fires only when numbers change. No retry. Knobs DB-driven via app_config insight.llm_url/llm_model/timeout(8s)/temperature(0.2) with legacy EMS_INSIGHT_* env fallbacks.

### stage `copilot_suggest (assigned — separate copilot service, no stage concept)` — /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/copilot/generate.py:56
- **purpose**: Pure-AI typeahead: given the user's partial query text plus retrieved real EMS entities, the 4B model composes the inline autofill continuation and 5 complete query suggestions (the model IS the source of suggestions — no template synthesis).
- **prompt_built_from**: System = hardcoded prompts.SYSTEM (Google-autocomplete-style completion rules, half-typed-word contract); user = prompts.build_user(text, grounding) listing real assets/metrics/times/areas/dashboards/cards/exemplar-questions from copilot_index.sqlite (built from cmd_catalog :5432 + neuract device_mappings :5433).
- **candidates**: Grounding entity set g = retrieve.retrieve(text) — Python dict of ranked lists {assets, metrics, times, areas, pages, cards, questions} materialized in copilot/retrieve.py before prompting (caps from config.RETRIEVE_PER_TYPE) and rendered into the user message by prompts.build_user (copilot/prompts.py:56-113). The model must use ONLY these entities.
- **selected_field**: autofill (string continuation) and suggestions (string array); post-processed: prefix-consistency guardrail on autofill, ghost = suffix, dedupe + _relevance_rank, truncated to MAX_SUGGESTIONS=5
- **reasoning_field**: none
- **confidence_field**: none
- **output_shape**: raw model: {"autofill": "...", "suggestions": ["q1".."q5"]}; generate() returns {autofill, ghost, suggestions[<=5], source: 'model'|'unavailable', error?}; server adds latency_ms + cached
- **error_handling**: Call convention: copilot/llm.py chat() — stdlib urllib POST to LLM_URL+/chat/completions where LLM_URL=http://localhost:8201/v1, model Qwen/Qwen3-4B-Instruct-2507-FP8 (copilot/config.py:13-14), temperature 0.3, max_tokens 0 (=uncapped, EOS stop), timeout 8s, response_format json_object; optional guided_json passthrough exists in chat()'s signature but no caller uses it. llm.chat itself does NOT catch — every caller wraps: generate() catches ANY exception → honest empty {source:'unavailable', error:<160 chars>}; server.suggest() caches ONLY genuine model results (never caches failures — anti-cache-poison). No retry. LOGGING: NO per-call AI logging anywhere — obs/ai_log is not imported by copilot AND its filter is ':8200' so it would ignore :8201 anyway; the only traces are process stdout logs in copilot/logs/ (server.log, vllm-copilot.log, build_llm_aliases.log). llm.is_up() probes GET /models for health.
- **notes**: Fully standalone service on :8772 (copilot/server.py, systemd deploy/ems-copilot.service); zero pipeline imports; frontend PromptBar POSTs /copilot/suggest.

### stage `copilot_starters (assigned)` — /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/copilot/starters.py:62
- **purpose**: The model CHOOSES 5 diverse real assets from the plant roster and composes the 5 empty-state starter commands, one per fixed intent (real-time / trend / V&I / power-quality / alarms).
- **prompt_built_from**: System = hardcoded _SYS in starters.py:18-38 (relevance rules: one asset per command, metric fits asset type, 5 distinct intents in fixed order); user = the real asset-by-class listing + instruction to choose n diverse assets.
- **candidates**: Real non-Spare assets grouped by class from the copilot index: _asset_listing(ix) at copilot/starters.py:41-49 builds by_class dict from ix.by_type['asset'] (retrieve.index()) and renders up to 10 names per class into the user message — the model must pick ONLY from these.
- **selected_field**: starters — array of {tag, text}; the chosen assets are embedded inside each composed command text
- **reasoning_field**: none
- **confidence_field**: none
- **output_shape**: {"starters": [{"tag": "<2-4 char mnemonic>", "text": "<command>"} x5]} — sanitized to 5 non-empty {tag[:4], text} entries
- **error_handling**: Via the same copilot llm.chat (:8201, temperature 0.5, timeout 30s, json_object); bare try/except → deterministic fallback roster (first asset of 5 distinct non-Spare classes into 5 templated commands). Result cached forever in module-global _CACHE (generated once per process, pre-warmed in server._warmup). No retry, no per-call logging (same :8201 blind spot).

### stage `copilot_alias_gen (assigned — offline/build-time)` — /home/rohith/desktop/BFI/backend/layer2/pipeline_v48/copilot/aliases.py:98
- **purpose**: Build-time generation of colloquial search synonyms (max 4) per EMS entity so the copilot index matches operator slang; one of three alias sources (curated / embedded / llm) applied in build/build_index.py.
- **prompt_built_from**: Hardcoded system string in gen_llm_aliases (aliases.py:91-96) + user = TSV listing 'id<TAB>kind<TAB>name' per chunk.
- **candidates**: The batch of entities to alias: items = [(entity_id, kind, display)] passed in and chunked 24-at-a-time (aliases.py:89-90, `chunk` list rendered as TSV lines into the user message).
- **selected_field**: the whole mapping — {"<entity_id>": ["syn1","syn2",...]} keyed by entity id; parsed into {int(id): [<=4 lowercased aliases]}
- **reasoning_field**: none
- **confidence_field**: none
- **output_shape**: {"<id>": ["syn1", "syn2", ...]} strict JSON, max 4 synonyms per item, items with none skipped
- **error_handling**: Same copilot llm.chat (:8201, temperature 0.4, max_tokens 900, timeout 30s, json_object); llm imported lazily inside the function so index builds succeed with the endpoint down; per-batch try/except continue — a failed batch is silently skipped (best-effort). Build stdout captured in copilot/logs/build_llm_aliases.log; no structured AI log.

**summary**: Complete inventory of AI call paths outside layer1a/layer1b/layer2 in /home/rohith/desktop/BFI/backend/layer2/pipeline_v48. FIVE real AI decision points reported as stages: (1) knowledge/ems.py knowledge_ems — the only one using the sanctioned llm/client.py call_qwen (stage='knowledge_ems', so it inherits :8200 obs/ai_log logging); (2) ems_exec/renderers/_insight.py — YES it POSTs to :8200 /v1/chat/completions DIRECTLY via urllib (line 122), bypassing call_qwen, but deliberately imports obs.ai_log whose urlopen monkeypatch still logs every :8200 request/response to outputs/logs/ai_<run_id>.jsonl; consumed only by ems_exec/renderers/narrative_ai.py (AI-summary cards 8/19/25/28 via host run_special dispatch); (3-5) the standalone copilot service (:8772) whose copilot/llm.py chat() is a stdlib urllib POST to its OWN :8201 Qwen3-4B endpoint (copilot/config.py) with THREE callers: generate.py (typeahead suggest), starters.py (empty-state commands), aliases.py gen_llm_aliases (offline alias build) — NONE of these are logged per-call anywhere (obs/ai_log filters on ':8200' and copilot never imports it; only process stdout logs exist in copilot/logs/: server.log, vllm-copilot.log, build_llm_aliases.log). NON-STAGES / negatives: knowledge/route.py DOES NOT EXIST — the knowledge dir contains only ems.py (route+answer+reject consolidated into one call) + prompts/ems.md, so memory's knowledge/{route,answer}.py is stale. run/degrade_gate.py makes NO LLM call — it only substring-matches the fingerprint 'llm transport/parse failure' (line 34) inside layer error text to convert a vLLM/:5433 outage into the honest data_unavailable page terminal; pure string policy, no transport. Whole-repo grep (excluding tests/, tools/, archive/, __pycache__) for urllib/requests/httpx/aiohttp POSTs found NO other bypass of call_qwen: validation/runner.py and validation/checks/datesync.py urlopen-POST to the pipeline's own HTTP API (/api/run, /api/frame at validation.config.BASE_URL) — not LLM endpoints (datesync explicitly documents /api/frame as no-LLM); llm/client.py:164 is call_qwen itself; obs/ai_log.py is the logging wrapper, not a caller. copilot/llm.py also exposes is_up() (GET /models health probe, non-generative).

## host

- **server_framework**: Pure Python stdlib `http.server` — no Flask/FastAPI. `/home/rohith/desktop/BFI/backend/layer2/pipeline_v48/host/server.py` defines `class Handler(BaseHTTPRequestHandler)` (protocol_version HTTP/1.1) served by `class _Server(ThreadingHTTPServer)` (request_queue_size=128, daemon_threads=True, allow_reuse_address=True), bound in `main()` via `_Server(("0.0.0.0", PORT), Handler)`. Port = 8770 (env `V48_HOST_PORT`). Routes are NOT declared declaratively — they are `self.path.startswith(...)` prefix checks inside `do_GET`/`do_POST`. Two examples, quoted: (1) in `do_GET`: `if self.path.startswith("/api/health"): return self._send(200, {"ok": True, "sb_base": SB_BASE})`; (2) in `do_POST`: `if self.path.startswith("/api/frame"):` ... and the run route is gated by `if not self.path.startswith("/api/run"): return self._send(404, {"ok": False, "error": "not found"})`. CORS is handled centrally in `Handler._send()` — every response gets `Access-Control-Allow-Origin: *`, `Access-Control-Allow-Headers: Content-Type`, `Access-Control-Allow-Methods: POST, GET, OPTIONS` — and `do_OPTIONS` answers 204 for preflight. So any new endpoint added via `_send` inherits CORS automatically.
- **pipeline_invocation**: `build_response(prompt, asset_id=None, date_window=None)` in host/server.py (line 100) is the single-asset runner: it calls `run_pipeline(prompt, asset_id=asset_id)` imported from `run.harness` (pipeline_v48/run/harness.py:184). Multi-asset compare goes through `host/multi_asset.build_response_multi(prompt, asset_ids, date_window=...)` instead. run_id: `run_pipeline` does `run_id = run_id or make_run_id(prompt)` then **yes — `ai_log.set_run_id(run_id)`** (harness.py:186-187; reflect-loop retries re-set it per attempt with `make_run_id(prompt, salt=f"loop{attempt}")`). Format (run/run_id.py): `r_` + sha1(f"{salt}|{prompt}")[:10] hex → e.g. `r_44796d791a` — deterministic from the prompt (no time/random). Artifacts keyed by run_id land in `pipeline_v48/outputs/logs/`: `response_<run_id>.json` (full /api/run response, persisted server-side by `_dump_response` before the wire write), `pipeline_<run_id>.jsonl` (obs.stage), `ai_<run_id>.jsonl` (obs.ai_log monkeypatch of urlopen for :8200 LLM calls). These files are the natural data source for an Inspector view.
- **existing_endpoints**: All in host/server.py Handler. GET: `/api/health` → {ok, sb_base}; `/api/assets?q=` → {ok, assets:[{mfm_id,name,class,load_group,has_data}]} (full lt_mfm registry for the picker, via layer1b.resolve.candidate_list.registry_for_picker); `/api/site` → {ok, site, live} (site name from app_config + a real `SELECT 1` probe of the live neuract DB). POST: `/api/run` body {prompt, asset_id?, asset_ids?, date_window?, history?} → the full pipeline response (knowledge prompts short-circuit to {kind:"knowledge", answer, refused}; ≥2 asset_ids or natural-compare → build_response_multi); `/api/frame` body {exact_metadata, data_instructions, refetch, date_window} → {ok, why, endpoint, payload} (per-card date re-fetch through fill_one_card). OPTIONS anything → 204 (CORS preflight). Anything else → 404 JSON. Separately, the FE also talks to `/copilot/*` which is a Vite proxy to the standalone copilot server on :8772, not part of server.py.
- **fe_structure**: `host/web/src/`: NO router library (package.json deps = react/react-dom only). `main.tsx` mounts `<App/>` inside an `AppBoundary` error boundary and imports `@cmd-v2/index.css` (CMD_V2 Tailwind v4 tokens, for imported cards only) then `./styles.css` (host chrome). `App.tsx` is a single-component state machine: `<CommandHeader onRun={...}/>` (always rendered, contains PromptBar + LIVE status) followed by a chained ternary over derived flags — `knowledge` → `<KnowledgeAnswer/>`, `outage` → `<DataUnavailable/>`, `hasCards` → `<CardGrid/>` (inside `DateSyncProvider` keyed by run_id), else empty state (`SuggestedCommands`/`RunningCard`) — plus a conditional `<AssetResolution/>` popup overlay when `resolving`. View switching = plain `useState` booleans/flags on the run result; there are no routes/URLs. Last completed dashboard persists to sessionStorage (`v48:last_dashboard`) but a hard reload intentionally clears it. `api.ts` conventions: thin `fetch` wrappers to RELATIVE paths (`/api/run`, `/api/frame`) so the Vite dev proxy handles the origin; `Content-Type: application/json` POST bodies; `if (!res.ok) throw new Error(body?.error || \`HTTP ${res.status}\`)`; typed against `types.ts` (`PipelineResult`, mirror of build_response). Dirs: `components/` = AssetResolution, CardGrid, CmdCard, CommandHeader, DataUnavailable, DateSync, KnowledgeAnswer, PromptBar, RtmComposite, SuggestedCommands; `layout/` = pure TS grid math (cellPos, gridPlan, pageGrid, regions, tracks, vocab); `cmd/` = CMD_V2 component registry/compose/fill/guards/shims. Styling: ONE global `styles.css` — Neuract light "cream paper" theme (NOT dark despite a stale comment in main.tsx): CSS custom properties on :root (`--cream-100` page bg, `--teal-900` ink, `--brown-600` sole interactive accent, `--coral-500` alarm-only, `--hairline`, `--font-mono` Space Mono for machine values / `--font-sans` Inter for words) and `cc-` prefixed classes; components use those classes plus occasional inline style objects for one-offs (see ValidationBar in App.tsx). Host chrome does NOT use Tailwind utility classes — Tailwind exists only to style the imported CMD_V2 cards. Shell layout gotcha: `.cc-shell` is `height:100vh; overflow:hidden` flex-column, so any full-page view must scroll internally (`.cc-work` is the existing `flex:1` + own-overflow workspace wrapper).
- **fe_add_view_recipe**: Consistent with the app's state-driven (router-less) switching. Files to touch: (1) NEW `/home/rohith/desktop/BFI/backend/layer2/pipeline_v48/host/web/src/components/InspectorView.tsx` — export `function InspectorView({ onBack }: { onBack: () => void })`; render inside its own scrollable container (the shell forbids page scroll). (2) `web/src/api.ts` — add typed fetch helpers for any new endpoints, following the existing pattern: relative `/api/inspector/...` URL, `res.json()`, `if (!res.ok) throw new Error(body?.error || \`HTTP ${res.status}\`)`. (3) `web/src/App.tsx` — add a view state next to the existing flags: `const [view, setView] = useState<'command'|'inspector'>(() => location.hash === '#inspector' ? 'inspector' : 'command');` plus a small `useEffect` that writes `location.hash = view === 'inspector' ? '#inspector' : ''` and listens to `hashchange` — hash-based client-side nav matches the no-router-dependency convention while making the view linkable/back-button-able (do NOT add react-router; package.json has no router and the app is deliberately minimal). Then in the JSX, branch ABOVE the existing ternary chain, keeping the header mounted: `<div className="cc-shell"><CommandHeader onRun={(p)=>{setView('command'); run(p);}} loading={loading} seed={seed} onOpenInspector={() => setView('inspector')} />{view === 'inspector' ? (<main className="cc-work"><div className="cc-work-inner"><InspectorView onBack={() => setView('command')} /></div></main>) : ( ...the existing knowledge/outage/hasCards/empty ternary unchanged... )}<PoweredByNeuract/>{resolving && ...}</div>` — reusing `<main className="cc-work">` gives the correct flex:1 internally-scrolling region for free. (4) `web/src/components/CommandHeader.tsx` — add an optional prop `onOpenInspector?: () => void` and render a small button between `.cc-barwrap` and `.cc-status` (e.g. `<button className="cc-nav-btn" onClick={onOpenInspector}>INSPECTOR</button>`); optional prop keeps every existing call site compiling. (5) `web/src/styles.css` — append a `/* ═══ INSPECTOR ═══ */` section using the existing vars and `cc-` prefix: e.g. `.cc-nav-btn { font-family: var(--font-mono); font-size: 11px; letter-spacing: .06em; color: var(--brown-600); background:#fff; border:1px solid var(--hairline); border-radius:6px; padding:6px 12px; cursor:pointer; } .cc-nav-btn:hover { border-color: var(--brown-300); }` and inspector panels on `#fff`/`var(--cream-200)` with `border:1px solid var(--hairline)` — light cream theme, brown = only interactive accent, coral only for errors, mono font for run_ids/values. (6) `web/src/types.ts` — add interfaces for the inspector API responses. (7) Backend (if the Inspector needs data): in `host/server.py` `do_GET`, add a prefix branch before the final 404, mirroring `/api/assets`: `if self.path.startswith("/api/inspector"): try: ... return self._send(200, {"ok": True, ...}) except Exception as e: traceback.print_exc(); return self._send(500, {"ok": False, "error": f"{type(e).__name__}: {e}"})` — CORS/OPTIONS already covered by `_send`/`do_OPTIONS`, and the Vite `/api` proxy forwards it with zero config changes. Natural inspector data source: `pipeline_v48/outputs/logs/` — `response_<run_id>.json`, `pipeline_<run_id>.jsonl`, `ai_<run_id>.jsonl` (list dir for run ids, parse per run). Nothing else needs touching: no router config, no vite.config change, no main.tsx change.
- **dev_serve**: `npm run dev` in `/home/rohith/desktop/BFI/backend/layer2/pipeline_v48/host/web` → Vite on port 5188 (`host: true`, so LAN-reachable). vite.config.ts proxies `/api` → `http://localhost:8770` (overridable via env `V48_HOST_API`) and `/copilot` → `http://localhost:8772` (`V48_COPILOT_API`), both `changeOrigin: true` — so the FE always fetches relative paths and never hardcodes the API origin. Also: `@cmd-v2` alias → `/home/rohith/CMD_V2/src` (read-only import of real CMD_V2 card components; that tree is watch-ignored so external CMD_V2 edits don't reload :5188), react/react-dom deduped/aliased to the host's node_modules, `fs.allow` includes the CMD_V2 dir, plugins = @vitejs/plugin-react + @tailwindcss/vite (Tailwind v4, for CMD_V2 card styles only). The API itself runs separately: `python3 host/server.py` (0.0.0.0:8770).
- **summary**: host/ preview app = stdlib http.server API (server.py, :8770, prefix-matched routes in do_GET/do_POST, blanket CORS in _send) + a router-less React/Vite FE (web/, :5188, /api proxied to :8770). Endpoints: GET /api/health, /api/assets?q=, /api/site; POST /api/run (calls build_response → run.harness.run_pipeline, which mints run_id = r_<sha1(prompt)[:10]> and calls obs.ai_log.set_run_id; response persisted to outputs/logs/response_<run_id>.json) and POST /api/frame (per-card date re-fetch). FE view switching is pure React state in a single App.tsx (knowledge/outage/cards/empty ternary + AssetResolution overlay), styled by one styles.css with Neuract cream/teal/brown CSS vars and cc- classes (no Tailwind for chrome). Recipe for an Inspector view: new components/InspectorView.tsx + api.ts helpers + a `view` useState in App.tsx (hash-synced for linkability, no router lib) branching to `<main className="cc-work">` with the header kept mounted, an optional onOpenInspector button prop on CommandHeader, cc-prefixed styles appended to styles.css, and (if needed) a new `/api/inspector` prefix branch in server.py do_GET reading outputs/logs/{response,pipeline,ai}_<run_id> files — no vite/proxy/CORS changes required.


## gaps critic

FAILED: session limit (resets 7am IST) — rerun before trusting coverage complete
