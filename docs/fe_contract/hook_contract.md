# hook_contract — DATA / METADATA / UI-selection three-boundary split

The payload carries only a read-only SEED of interaction state; writes flow UP via setters; one setter → all interdependent useMemos recompute (Approach B). **PROVISIONAL — frontend interdependency wiring STILL IN PROGRESS.**
