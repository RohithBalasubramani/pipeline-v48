"""grounding/ — deterministic PRE units (NO AI). Each engine reads a cmd_catalog config table + probes the live
neuract tables to build a card's fact-sheet. ZERO hardcoded thresholds/mappings — every policy comes from config/*."""
